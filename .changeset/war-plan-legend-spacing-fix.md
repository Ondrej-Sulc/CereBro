Updates War Attack Plan overview image with improved legend spacing and fallbacks.

-   **MapImageService**:
    -   Increased legend width to 1200px.
    -   Adjusted column offsets to prevent "Path" text from overlapping "Team" images.
    -   Added a fallback visual (colored ring) for assigned champions with missing images.
    -   Increased font sizes for better legibility.
