Updates War Attack Plan overview image generation.

- Modifies `MapImageService` to include `assignedChampions` in the legend.
- Updates `LegendItem` interface and image caching logic.
- Adjusts SVG generation to render champion images after player names.
- Updates `distributeWarPlan` to populate `assignedChampions` from fight data.
